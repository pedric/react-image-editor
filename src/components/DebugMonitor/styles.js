import styled from "styled-components";

const Div = styled.div`
  background: rgba(0, 0, 0, 0.75);
  font-size: 12px;
  padding: 5px;
  position: fixed;
  top: 0;
  right: 0;
  z-index: 1;

  pre {
    color: cyan;
  }
`;

export default Div;
